name: Validar Backend (CI)

on:
  push:
    branches: [ "main", "test", "develop" ]
  pull_request:
    branches: [ "main", "test", "develop" ]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pruebas:
    name: Pruebas y compilación
    runs-on: ubuntu-latest
    
    # 1. Agregamos un servicio temporal de PostgreSQL
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: gutti
          POSTGRES_PASSWORD: gutti123
          POSTGRES_DB: gutti_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    defaults:
      run:
        working-directory: ./backend
        
    steps:
      - uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Compilar y correr Pruebas
        run: mvn clean test
